FROM nginx:alpine
WORKDIR /var/www

ARG SERVER_NAME
ARG APP_NAME
ARG APP_ENV

ARG LC_ALL
ARG LANG
ARG TZ

ENV SERVER_NAME="${SERVER_NAME}"
ENV APP_NAME="${APP_NAME}"
ENV APP_ENV="${APP_ENV}"
ENV APP_SECRET="${APP_SECRET}"

ENV LC_ALL=${LC_ALL}
ENV LANG=${LANG}
ENV TZ=${TZ}

CMD ["nginx"]
EXPOSE 80
